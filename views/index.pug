extends layout

block content
  h1= title
  p Welcome to F.R.I.E.N.D.S #{title}
  br
  - var btnType = 'danger'
  - var btnSize = 'lg'
  button(type='button' class='btn btn-' + btnType + ' btn-' + btnSize) Press Me
